#include<stdio.h>
int main(){
    int km, m, pound, kg, converse;
    printf("give your converstion \n");
    scanf("%d", &converse);
    printf("you gave this to convert %d" , converse);
    printf("distance in meters will be %f", (km) * (100.00));
}